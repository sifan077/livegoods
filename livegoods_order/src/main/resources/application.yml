server:
  port: 9009
spring:
  application:
    name: livegoods-order
  profiles:
    active: mongo
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/
  instance:
    prefer-ip-address: true
